interface:
  display_name: "Execute Ticket Graph"
  short_description: "Run ticket graphs with parallel implement agents"
  default_prompt: "Use $execute-ticket-graph to read this to-tickets graph and run each ticket through implement in dependency order."
